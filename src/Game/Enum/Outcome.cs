namespace Game.Enum
{
    public enum Outcome
    {
        /// <summary>
        /// A game that has ended with a win
        /// </summary>
        Win,

        /// <summary>
        /// A game that has ended with a draw
        /// </summary>
        Draw,

        /// <summary>
        /// A game that has ended with a loss
        /// </summary>
        Lose
    }
}
